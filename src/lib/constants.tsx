// Імпортуємо тип ShipsConfig, який описує конфігурацію кораблів
import { ShipsConfig } from '../types';

// Константа з типовим набором кораблів:
// Кожен елемент масиву — це пара [розмір_корабля, кількість_кораблів]
// [4, 1] — один чотирипалубний
// [3, 2] — два трипалубних
// [2, 3] — три двопалубних
// [1, 4] — чотири однопалубних
export const DEFAULT_SHIPS_CONFIG: ShipsConfig = [
  [4, 1],
  [3, 2],
  [2, 3],
  [1, 4],
];

// Типовий розмір ігрового поля (10x10)
export const DEFAULT_FIELD_SIZE = 10;
